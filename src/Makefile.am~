NULL = 

AM_CPPFLAGS = \
	$(SUBPARSONIC_CFLAGS) \
	-include $(CONFIG_HEADER) \
	$(NULL)

bin_PROGRAMS = subparsonic

subparsonic_SOURCES = \
	MediaKeys.vala PlaybackView.vala Proxy.vala Song.vala PlaybackModel.vala \
        StreamPlayer.vala LoginView.vala LoginModel.vala main.vala \
	$(NULL)

subparsonic_VALAFLAGS = --pkg libsoup-2.4 --pkg json-glib-1.0 --pkg gee-1.0 --pkg gtk+-3.0 --pkg \
	 gstreamer-0.10 --pkg gstreamer-interfaces-0.10 --pkg gio-2.0 --pkg libnotify \
	--disable-warnings

subparsonic_LDADD = \
	$(SUBPARSONIC_LIBS) \
	$(NULL)

EXTRA_DIST = \
	$(NULL)

DISTCLEANFILES = \
	$(NULL)

